# customer-portal

Customer-facing React application for Fresh Greens.

## Commands

- `npm run dev` — start customer portal locally
- `npm run build` — typecheck + production build
- `npm run preview` — preview production build

## Naming Policy

Only these frontend application names are valid in this repository:

- `customer-portal`
- `admin-console`
